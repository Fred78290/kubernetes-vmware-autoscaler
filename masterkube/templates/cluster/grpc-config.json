{
    "address": "$CONNECTTO",
    "secret": "AutoScaler",
    "timeout": 300,
    "config": {
        "kubeAdmAddress": "$MASTER_IP",
        "kubeAdmToken": "$TOKEN",
        "kubeAdmCACert": "sha256:$CACERT",
        "kubeAdmExtraArguments": [
            "--ignore-preflight-errors=All"
        ]
    }
}